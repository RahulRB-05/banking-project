spring:
  application:
    name: Senshi

  datasource:
    url: jdbc:mysql://localhost:3306/banking?createDatabaseIfNotExist=true
    username: root
    password: Muruga6
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
     hibernate:
            ddl-auto:update
            show.sql:true
     properties:
                hibernate:
                         format_sql: true
